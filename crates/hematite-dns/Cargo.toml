# L2 DNS interception server (spec Part 06). Hand-rolled DNS wire handling
# keeps the dependency budget closed (Appendix E lists hickory-proto, but
# the A/AAAA/CNAME subset we serve is small enough to own).
[package]
name = "hematite-dns"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hematite-kernel.workspace = true
tokio.workspace = true
